# Docs: https://github.com/Azure/webapps-deploy
# Purpose: Build and deploy a Node.js app (frontend or fullstack) to Azure Web App

name: Build and Deploy Node.js App to Azure Web App - MkToDoApp

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # If your project uses Vite/React/Next/etc., this will create the build output in ./dist or ./build
      - name: Build project
        run: npm run build --if-present

      # Optional: Run tests if present
      - name: Run tests
        run: npm test --if-present

      # Upload only the build output (not the entire repo)
      - name: Upload artifact for deployment
        uses: actions/upload-artifact@v4
        with:
          name: node-build
          path: |
            dist
            build
            package.json
            package-lock.json
            server.js
            app.js
            index.js
          if-no-files-found: ignore

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: node-build

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_89548B53D1E24DDFAB55EED5C04786CE }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_75E0E493D3F64D429DB96C7F31C1AD39 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_36EEE81C468A4B01BC61370F9708C8E4 }}

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'MkToDoApp'
          slot-name: 'Production'
          package: .
